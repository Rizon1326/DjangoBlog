pip install django
pip install djangorestframework
pip install django-cors-headers
pip install celery
pip install redis
pip install django-celery-beat
pip install jwt
pip install django-allauth
pip install django-allauth-jwt
pip install django-filter
pip install markdown
